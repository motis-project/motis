include "paxmon/PaxMonInterchangeInfo.fbs";

namespace motis.paxmon;

table PaxMonCriticalInterchangesResponse {
  total_matching_interchanges: ulong;
  interchanges_in_this_response: ulong; // number of interchanges included in response
  remaining_interchanges: ulong; // number of remaining matching interchanges
  next_skip: ulong; // skip_next parameter for next call

  interchanges: [PaxMonInterchangeInfo];
}
